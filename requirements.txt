# Web UI Agent 依赖清单
# 安装命令: pip install -r requirements.txt

# 核心依赖
langgraph>=0.1.0
langchain-openai>=0.1.0
playwright>=1.40.0
beautifulsoup4>=4.12.0

# 可选：环境变量管理（需要创建 .env 文件时使用）
python-dotenv>=1.0.0
